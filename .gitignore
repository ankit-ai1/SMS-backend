# local secrets — never commit
.env

# generated dev keypair
dev_priv.pem
dev_pub.pem

node_modules/
dist/

# uploaded documents (local storage driver)
var/
branch_structure.json
temp_auto_push.bat
temp_interactive_push.bat
.gitignore
